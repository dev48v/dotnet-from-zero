# Copy this file to `.env` and replace DEMO_KEY with a real key
# from https://api.nasa.gov/. DEMO_KEY works for ~30 calls/hour
# total (across the whole world), so a personal key is safer.
NASA_API_KEY=DEMO_KEY
